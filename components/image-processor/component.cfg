logging = {
      level = {
          app = "trace"
          opcua = "warning"
      }
      path = "logs"
}

// OPC UA Server configuration

opcua = {
    port = 4843
    encryption = false
    anonymous = true
}

lds-uri = "pnp.component.mes"

image_processor = { 
    outputImage_resize_height = 640
    err_ratio = 10
    draw_markers = true
    output_image = true
}