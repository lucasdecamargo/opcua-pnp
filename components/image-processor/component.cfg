logging = {
      level = {
          app = "trace"
          opcua = "warning"
      }
      path = "logs"
}

// OPC UA Server configuration

opcua = {
    port = 4843
    encryption = false
    anonymous = true
}

lds-uri = "pnp.mes"

image_processor = { 
    outputImage_resize_height = 300
    err_ratio = 100
    draw_markers = true
    output_image = true
    load_sample_parameters = true
}