logging = {
      level = {
          app = "debug"
          opcua = "warning"
      }
      path = "logs"
}

// OPC UA Server configuration

opcua = {
    port = 4842
    encryption = false
    anonymous = true
}

lds-uri = "pnp.component.mes"

camera = { }